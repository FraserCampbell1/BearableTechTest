import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 150,
    alignItems: "center",
  },
  title: {
    fontSize: 16,
  },
  welcomeText: {
    fontSize: 16,
  },
});

export default styles;
