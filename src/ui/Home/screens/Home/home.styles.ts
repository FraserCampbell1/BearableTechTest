import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#FFF",
  },

  activityIndicator: {
    marginTop: 100,
  },

  contentContainer: {
    marginTop: 150,
    alignItems: "center",
  },
  title: {
    fontSize: 16,
  },
  welcomeText: {
    fontSize: 16,
  },
});

export default styles;
